import type { Config } from "tailwindcss";

export const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: {
          default: "#3e3e3e",
          200: "#3e3e3e",
        },
      },
    },
  },
};
